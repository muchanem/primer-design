package main

import (
	"testing"
)
 
func TestgetFile(t *testing.T) {
	text := getFile("testing")
	basePairs := ""
    if ans != basePairs {

        t.Errorf("IntMin(2, -2) = %d; want -2", ans)
    }
}

